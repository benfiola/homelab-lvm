# homelab-lvm

This repo provides a docker image containing a custom built `lvm` binary for use with the [homelab-helper](https://github.com/benfiola/homelab-helper.git) project as a means to reduce overall build times.
